/gpt 라는 Post메서드를 만들고, 해당 메서드는 body에 있는 text필드를 받아.                                                                            
  그리고 연결된 웹소켓(최대 1개만 연결 가능)을 통해서 외부에 보내.                                                                                     
  그다음에 /gpt/response 라는 Post요청 (그 바디안에 text필드있음)이 오면                                                                               
  해당 gpt라는 처음에 보낸 post요청에게 받은 text를 담은 response를 보냄.

   내가 브라우저 콘솔에다가 직접 특정 코드를 복사 붙여넣기 할거야.                                                                                      
  해당 웹은 로컬호스트가 아니라 CORS설정을 전부 켜줘야함. 
  이때 붙여넣기하면 해당 웹이랑 내 서버랑 웹소켓으로 연결되게 해야함.

  외부에서 여러번 /gpt 를 호출하면 FIFO 큐에 담겨서 감당 가능하게 함.
  API에는 CORS를 전체 허용해야 함